# pip install -r requirements.txt
# Create virtual env in .venv folder
python -m venv .venv

source .venv/bin/activate
# Require for the very first time when codespace is created
git remote add origin https://github.com/masabai/etl-pipeline-testing.git

# create a branch from cs
git checkout -b dev

# push - need only once
git push --set-upstream origin dev 

# Start working in Dev, commit changes
git add .
git commit -m "Add transform test"

# After the first push …and it will automatically push to origin/dev
git push

# merge dev -> main
git checkout main
git merge dev
git push


# clean up ghose files
sudo apt install git-filter-repo
git filter-repo --path data/raw --path data/processed --invert-paths

#update .gitigore
/data/raw/
/data/processed/


# untrack
git add .gitignore
git commit -m "Ignore raw and processed data"


git filter-repo --path data/raw --path data/processed --invert-paths --force


